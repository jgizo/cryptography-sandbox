# math_helpers.py
def mod_exp(base: int, exponent: int, modulus: int) -> int:
    # TODO: Efficient modular exponentiation
    pass

def extended_gcd(a: int, b: int) -> tuple:
    # TODO: Return (g, x, y) such that a*x + b*y = g = gcd(a, b)
    pass
